# File generated by jrohc. DO NOT EDIT.
openapi: 3.0.0
info:
  title: Models
  version: ''
paths: {}
components:
  schemas:
    createProfilesInBulkParams:
      type: object
      properties:
        profiles:
          type: array
          minItems: 1
          items:
            allOf:
            - $ref: '#/components/schemas/profile'
            - description: Details of the profile.
      required:
      - profiles
    createProfilesInBulkResp:
      type: object
      properties:
        traceID:
          type: string
          description: The identifier of the trace associated with the log entry.
          example: Uv38ByGCZU8WP18PmmIdcg
        error:
          allOf:
          - $ref: ../common.yaml#/components/schemas/error
          - description: The error encountered. This field is mutually exclusive of
              the `results` field.
        results:
          allOf:
          - $ref: '#/components/schemas/createProfilesInBulkResults'
          - description: The results returned. This field is mutually exclusive of
              the `error` field.
      required:
      - traceID
    createProfilesInBulkResults:
      type: object
      properties:
        profileIDs:
          type: array
          items:
            allOf:
            - $ref: '#/components/schemas/profileID'
            - description: The identifier of the profile.
    getProfilesInBulkParams:
      type: object
      properties:
        profileIDs:
          type: array
          minItems: 1
          items:
            allOf:
            - $ref: '#/components/schemas/profileID'
            - description: The identifier of the profile.
      required:
      - profileIDs
    getProfilesInBulkResp:
      type: object
      properties:
        traceID:
          type: string
          description: The identifier of the trace associated with the log entry.
          example: Uv38ByGCZU8WP18PmmIdcg
        error:
          allOf:
          - $ref: ../common.yaml#/components/schemas/error
          - description: The error encountered. This field is mutually exclusive of
              the `results` field.
        results:
          allOf:
          - $ref: '#/components/schemas/getProfilesInBulkResults'
          - description: The results returned. This field is mutually exclusive of
              the `error` field.
      required:
      - traceID
    getProfilesInBulkResults:
      type: object
      properties:
        profiles:
          type: array
          items:
            allOf:
            - $ref: '#/components/schemas/profile'
            - description: Details of the profile.
    updateProfilesInBulkParams:
      type: object
      properties:
        profilePatches:
          type: array
          minItems: 1
          items:
            allOf:
            - $ref: '#/components/schemas/profilePatch'
            - description: The patch to apply to the profile.
      required:
      - profilePatches
    updateProfilesInBulkResp:
      type: object
      properties:
        traceID:
          type: string
          description: The identifier of the trace associated with the log entry.
          example: Uv38ByGCZU8WP18PmmIdcg
        error:
          allOf:
          - $ref: ../common.yaml#/components/schemas/error
          - description: The error encountered. This field is mutually exclusive of
              the `results` field.
        results:
          allOf:
          - $ref: '#/components/schemas/updateProfilesInBulkResults'
          - description: The results returned. This field is mutually exclusive of
              the `error` field.
      required:
      - traceID
    updateProfilesInBulkResults:
      type: object
      properties:
        updatedProfileIDs:
          type: array
          items:
            allOf:
            - $ref: '#/components/schemas/profileID'
            - description: The identifier of the profile.
    deleteProfilesInBulkParams:
      type: object
      properties:
        profileIDs:
          type: array
          minItems: 1
          items:
            allOf:
            - $ref: '#/components/schemas/profileID'
            - description: The identifier of the profile.
      required:
      - profileIDs
    deleteProfilesInBulkResp:
      type: object
      properties:
        traceID:
          type: string
          description: The identifier of the trace associated with the log entry.
          example: Uv38ByGCZU8WP18PmmIdcg
        error:
          allOf:
          - $ref: ../common.yaml#/components/schemas/error
          - description: The error encountered. This field is mutually exclusive of
              the `results` field.
        results:
          allOf:
          - $ref: '#/components/schemas/deleteProfilesInBulkResults'
          - description: The results returned. This field is mutually exclusive of
              the `error` field.
      required:
      - traceID
    deleteProfilesInBulkResults:
      type: object
      properties:
        deletedProfileIDs:
          type: array
          items:
            allOf:
            - $ref: '#/components/schemas/profileID'
            - description: The identifier of the profile.
    createProfileParams:
      type: object
      properties:
        profile:
          allOf:
          - $ref: '#/components/schemas/profile'
          - description: Details of the profile.
      required:
      - profile
    createProfileResp:
      type: object
      properties:
        traceID:
          type: string
          description: The identifier of the trace associated with the log entry.
          example: Uv38ByGCZU8WP18PmmIdcg
        error:
          allOf:
          - $ref: ../common.yaml#/components/schemas/error
          - description: The error encountered. This field is mutually exclusive of
              the `results` field.
        results:
          allOf:
          - $ref: '#/components/schemas/createProfileResults'
          - description: The results returned. This field is mutually exclusive of
              the `error` field.
      required:
      - traceID
    createProfileResults:
      type: object
      properties:
        profileID:
          allOf:
          - $ref: '#/components/schemas/profileID'
          - description: The identifier of the profile.
      required:
      - profileID
    getProfileParams:
      type: object
      properties:
        profileID:
          allOf:
          - $ref: '#/components/schemas/profileID'
          - description: The identifier of the profile.
      required:
      - profileID
    getProfileResp:
      type: object
      properties:
        traceID:
          type: string
          description: The identifier of the trace associated with the log entry.
          example: Uv38ByGCZU8WP18PmmIdcg
        error:
          allOf:
          - $ref: ../common.yaml#/components/schemas/error
          - description: The error encountered. This field is mutually exclusive of
              the `results` field.
        results:
          allOf:
          - $ref: '#/components/schemas/getProfileResults'
          - description: The results returned. This field is mutually exclusive of
              the `error` field.
      required:
      - traceID
    getProfileResults:
      type: object
      properties:
        profile:
          allOf:
          - $ref: '#/components/schemas/profile'
          - description: Details of the profile.
      required:
      - profile
    updateProfileParams:
      type: object
      properties:
        profilePatch:
          allOf:
          - $ref: '#/components/schemas/profilePatch'
          - description: The patch to apply to the profile.
      required:
      - profilePatch
    updateProfileResp:
      type: object
      properties:
        traceID:
          type: string
          description: The identifier of the trace associated with the log entry.
          example: Uv38ByGCZU8WP18PmmIdcg
        error:
          allOf:
          - $ref: ../common.yaml#/components/schemas/error
          - description: The error encountered.
      required:
      - traceID
    deleteProfileParams:
      type: object
      properties:
        profileID:
          allOf:
          - $ref: '#/components/schemas/profileID'
          - description: The identifier of the profile.
      required:
      - profileID
    deleteProfileResp:
      type: object
      properties:
        traceID:
          type: string
          description: The identifier of the trace associated with the log entry.
          example: Uv38ByGCZU8WP18PmmIdcg
        error:
          allOf:
          - $ref: ../common.yaml#/components/schemas/error
          - description: The error encountered.
      required:
      - traceID
    profile:
      type: object
      properties:
        id:
          allOf:
          - $ref: '#/components/schemas/profileID'
          - description: The identifier of the profile. It's ignored while creating
              a profile.
        nickname:
          allOf:
          - $ref: '#/components/schemas/nickname'
          - description: The nickname.
        genderType:
          allOf:
          - $ref: '#/components/schemas/genderType'
          - description: |-
              The type of gender.

              Constants:

              - GENDER_FEMALE(1): The gender is female.
              - GENDER_MALE(2): The gender is male.
              - GENDER_OTHER(3): Other genders.
        weight:
          allOf:
          - $ref: '#/components/schemas/weight'
          - description: The weight in kilograms.
        height:
          allOf:
          - $ref: '#/components/schemas/height'
          - description: The height in centimeters.
      required:
      - id
      - nickname
      - genderType
    profilePatch:
      type: object
      properties:
        id:
          allOf:
          - $ref: '#/components/schemas/profileID'
          - description: The identifier of the profile to update.
        nickname:
          allOf:
          - $ref: '#/components/schemas/nickname'
          - description: The nickname. This field should be given only if it needs
              to be changed.
        genderType:
          allOf:
          - $ref: '#/components/schemas/genderType'
          - description: |-
              The type of gender. This field should be given only if it needs to be changed.

              Constants:

              - GENDER_FEMALE(1): The gender is female.
              - GENDER_MALE(2): The gender is male.
              - GENDER_OTHER(3): Other genders.
        weight:
          allOf:
          - $ref: '#/components/schemas/weight'
          - description: The weight in kilograms. This field should be given only
              if it needs to be changed.
        height:
          allOf:
          - $ref: '#/components/schemas/height'
          - description: The height in centimeters. This field should be given only
              if it needs to be changed.
      required:
      - id
    profileID:
      type: integer
      format: int64
      minimum: 1
      example: 1000
    nickname:
      type: string
      minLength: 1
      maxLength: 50
      example: Lee
    genderType:
      type: integer
      format: int32
      enum:
      - 1
      - 2
      - 3
    weight:
      type: number
      format: double
      minimum: 0.0
      exclusiveMinimum: true
      example: 60.5
    height:
      type: integer
      format: int32
      minimum: 1
      example: 177
